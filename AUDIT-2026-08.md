# Match Lens — Audit, August 2026

Whole-repo audit: 43 Python modules (~21,900 LOC) plus `SKILL.md` (4,874 lines).

---

## Verification tiers

Findings are split by how strongly they are evidenced. This distinction is the
point of the document — the previous audit's headline findings had gone stale
without anyone noticing, so provenance is tracked explicitly.

- **CONFIRMED** — re-derived by reading the cited code directly during this audit.
  Every quote checked against the working tree.
- **REPORTED** — surfaced by a systematic sweep with file:line evidence and a
  traced failure path, but not independently re-derived. Treat as high-quality
  leads: verify before fixing.

No match data, video, or installable environment exists here, so **the pipeline
cannot be executed**. This is static analysis throughout — findings are argued
from code paths, never from a reproduction run.

Two candidates that did not survive verification were **dropped**, not downgraded
and kept. They are listed at the end so a future audit does not re-raise them.

---

## Summary

The pipeline's correctness machinery is largely **written but not wired**.

Three mechanisms that exist to keep the analysis honest do not do their job: the
readiness gate is never read (**A1**), per-finding evidence stamping runs on an
array the agent schema never emits (**A2**), and two published per-player metrics
are constants computed from a field the spec forbids (**A3**).

**A1 and A3 are now fixed** — see their entries. A2 remains open because it needs a
design decision, not a patch.

Separately, several metrics **ship a provenance string that states a formula the
code does not implement** (**A5**, **A6**) — so the discrepancy is invisible in
both `deep_skill_metrics.json` and the report's methodology text.

The root cause is structural: `SKILL.md` governs 13 pipeline steps but names only
8 of 43 scripts by filename, and six steps bind to no module at all, so spec and
code can drift with nothing to detect it (**A11**).

---

# CONFIRMED

## A1 — `report_ready: false` does not stop report generation

**Severity: CRITICAL. Status: FIXED** (`_report_gate_open`, `pipeline_runner_v2.py:2005`).

`SKILL.md:2823` states the interlock in the strongest terms the spec uses:

> **Hard rule:** If `report_ready` is `false`, Step 4 does not run.

repeated at `SKILL.md:3600` and `SKILL.md:4812`. **No code enforces it.** A
repo-wide grep for `report_ready` returns hits in exactly one file —
`build_readiness_check.py`, which computes and writes it. Zero readers elsewhere.

The gate runs as step `3j_readiness` (`pipeline_runner_v2.py:2698`); its return
value is discarded by the generic step runner:

```python
func(match_dir)
mark_step(match_dir, state, step_id, "complete")   # :2349-2350
```

Control falls into `# ── PHASE 5: Reports ──` (`:2712`) — tactical report (`:2731`),
opposition report (`:2751`) — and `PHASE 6` (`:2807`) — flagged moments, pass
network. Phase 5 *does* load `report_readiness.json` (`:2923`, `:3035`) but reads
only `player_id_ceiling` from it.

**Failure:** boundary confidence 0.4, 12 of 20 windows complete → `report_ready:
false`, `[X]  NOT READY` prints, `False` returned and dropped, step marked
`complete`, all four reports generated and delivered carrying normal confidence
language. The mechanism the system relies on to avoid shipping unsupported
analysis never fires.

**Fix applied.** `_report_gate_open(match_dir, override)` reads
`report_readiness.json` after the metric steps and gates all three report
producers on it: `3l_synthesis`, PHASE 5 (4a/4b) and PHASE 6 (4c/4d) — 4c/4d
included because `flagged_moments.md` and `pass_network.md` are delivered
artefacts too. It **fails closed**: a missing or unreadable readiness file blocks,
since the gate not having run is not evidence the pipeline is healthy.
`--override-readiness` is the deliberate escape hatch — it proceeds, says so
loudly, and records `readiness_overridden: true` in `report_readiness.json` so the
artefacts stay traceable to a human decision. It is a separate flag from
`--force-reports`, which already means something else (use the older single-call
generators) and must not silently weaken the gate.

Verified against five scenarios: `ready=true` → open; `ready=false` → closed with
blocking issues listed; file missing → closed; corrupt JSON → closed; override →
open and recorded.

## A2 — merge-stage evidence stamping operates on an array that is always empty

**Severity: CRITICAL.**

`SKILL.md:1291` mandates the Universal Finding Object: *"Every finding produced by
agents must carry these fields"* — `finding_type`, `subject_player_id`,
`subject_player_label`, `supporting_frames`, `opposition_focus_type`,
`result_family_status`, `evidence_tier`.

The actual Tier-1 agent output schema (`pipeline_runner_v2.py:441-561`) has **no
`findings` array at all** — `grep -c '"findings"' pipeline_runner_v2.py` = **0**.

Per-field occurrence counts across all `.py` files:

| Field | Hits |
|---|---|
| `finding_type` | 0 |
| `subject_player_id` | 0 |
| `subject_player_label` | 0 |
| `supporting_frames` | 0 |
| `opposition_focus_type` | 0 |
| `evidence_tier` | **40** |
| `result_family_status` | **20** |

So the object's *identity and provenance* fields exist nowhere, while its two
*grading* fields are live — they are set on other structures (e.g.
`build_deep_skill_metrics_v2.py:126` derives `result_family_status` from the gate
file). An earlier revision of this document claimed all seven were absent; that
was wrong and is corrected here.

Yet `merge_utils.py:251-252` is built entirely around it:

```python
a["findings"] = stamp_gates_on_findings(
    a.get("findings", []), gates, source_note
)
```

**Failure:** `a.get("findings", [])` is always `[]`, so `stamp_gates_on_findings`
runs over an empty list on every window of every match. `SKILL.md:1320-1321`
("The pipeline stamps `result_family_status` and `source_type` from
`result_family_gates.json` after each window is merged") describes something that
never happens **at the merge stage**.

**Bounding the claim:** because `evidence_tier` and `result_family_status` are
live on other structures, this does *not* mean all family-keyed gating is inert.
What is inert is specifically the **per-window, per-finding** stamping — the path
that would attach a source-limitation note to an individual observation at merge
time. Gating that happens later, on aggregate metrics, still works. Report writers
receive window observations with no evidence tier and no limitation note; metrics
are separately graded.

That distinction matters for the fix: the choice in remediation item 3 is whether
to make the agents emit `findings`, or to delete the merge-stage stamping — not
whether the whole grading system is dead. It is not.

## A3 — two published player metrics are constants, from a field the spec forbids

**Severity: CRITICAL. Status: FIXED** (`deep_skill_metrics.py:1611`) — but see the spec contradiction below.

`deep_skill_metrics.py:1611`

```python
by_player[pid]["ratings"].append(obs.get("rating", 3))
```

`"rating"` occurs in **exactly one place in the entire repository** — that default.
No schema, no prompt, no agent emits it. And `SKILL.md` explicitly forbids it:

> `SKILL.md:1630` — "Describe what you saw specifically -- no evaluation, no ratings"
> `SKILL.md:4196` — "No ratings. No evaluation. No comparison to ideal performance."

So every observation contributes a fabricated `3`. With all ratings identical,
`var = 0.0` and `avg_r = 3.0`, giving for **every player in every match**:

- `player_role_consistency = round(1.0 - 0/4, 2)` = **1.00** ("perfectly consistent")
- `player_positioning_stability = round(3.0/5.0, 2)` = **0.60**

Emitted at `:2463-2469` with `evidence_tier="repeated_pattern"` and confidence ≈0.7.
The `if not ratings: continue` guard at `:1616` never fires, because the default
fabricates a value per observation. These are not degraded measurements — they are
constants presented as per-player findings.

**Fix applied.** The `, 3` default is removed; only real ratings are collected, and
both metrics emit `value: None` / `value_type: "unavailable"` with a
`limitation_note` when none exist — matching the file's `_unavailable()` idiom and
`SKILL.md:3200`. `observations_count` now counts observations rather than ratings
(previously identical only because the default appended one per observation), and
a `ratings_count` field was added so the two stay distinguishable.

**Spec contradiction this exposes — needs a decision.** `SKILL.md:3247-3248`
*specifies* both metrics as rating-derived ("Rating variance across observations",
"Average rating normalised to 0-1") while `SKILL.md:1630` and `:4196` forbid the
player agent from emitting ratings at all. The metrics were specified against a
field the pipeline was never permitted to produce. With the fabrication removed
they will now be honestly unavailable **on every match**, which is correct but
makes the underlying contradiction visible. Resolving it is a product decision:
either permit a bounded rating in the player schema, or retire these two metrics
and replace them with something derivable from observation text. Until then they
should be treated as permanently unavailable, not as a regression.

## A4 — ground truth validation scores every event as missed

**Severity: HIGH.**

`ground_truth.py:102` reads `moment.get("timestamp", "")`. `key_moments` entries
do not carry `timestamp` — the agent schema emits `minute`
(`pipeline_runner_v2.py:540`), and `merge_utils.py:400-403` states the live
contract:

```python
# Accept both "timestamp" (legacy) and "minute" (Fix 32a schema) keys.
return m.get("timestamp") or m.get("minute") or ""
```

`ground_truth.py` reads the legacy key **only**, with no fallback, so it receives
`""` for every moment, `seconds_match(x, "")` is always `False`, and every known
event scores *missed*.

**Failure:** the pipeline correctly detects all 3 goals; `ground_truth_check.json`
reports `missed: 3`. The event-validation layer is blind and reports a perfect run
as a total failure.

## A5 — `build_up_route_diversity` divides by 4 while reporting `/12`

**Severity: HIGH. Status: PROVENANCE FIXED; value decision OPEN.**

| | |
|---|---|
| `SKILL.md:3225` | "Distinct zone-to-zone route pairs / **12** (recalibrated ceiling)" |
| `deep_skill_metrics.py:1444` | `score = round(min(len(zone_pairs) / 4.0, 1.0), 2)` |
| `deep_skill_metrics.py:2372` | emits `calculation_basis`: `"Distinct zone-to-zone route pairs / 12 (recalibrated ceiling)"` |

Diversity saturates at 1.0 from **4** distinct routes instead of 12. Because
`calc_predictability` (`:1453`) is `pattern_reliability*0.6 + (1.0 - route_diversity)*0.4`,
almost any match with ≥4 route pairs is scored maximally *unpredictable*:
`predictability_score` is deflated by up to 0.4 and the `highly_predictable`
category (≥0.75) is effectively unreachable.

The `calculation_basis` field is provenance shipped to the report writer — it
asserts the spec formula, so the divergence is invisible downstream.

## A6 — `compactness_score` uses different weights *and* measures a different quantity

**Severity: HIGH. Status: PROVENANCE FIXED; value decision OPEN.**

| | |
|---|---|
| `SKILL.md:3208` | "Line height **stability** (60%) + pressing intensity (40%)" |
| `deep_skill_metrics.py:1069-1070` | `# 80% line height position, 20% pressing activity`<br>`score = round(height_score * 0.80 + press_norm * 0.20, 2)` |
| `deep_skill_metrics.py:2267` | emits `calculation_basis`: `"Line height stability (60%) + pressing intensity (40%)"` |

Two divergences at once. Pressing contributes **half** the weight the spec
assigns. And `height_score` is derived from `avg_height_pct` — absolute line
*position*, not stability; the docstring at `:1052` says so outright ("measured
from actual average line height, not stability of movement"). A high, stable block
therefore scores as *not* compact.

As with A5, the emitted provenance string states the spec formula.

**Fix applied to the provenance, not the values.** The weights and the route
ceiling are now module-level constants (`COMPACTNESS_W_HEIGHT`,
`COMPACTNESS_W_PRESSING`, `ROUTE_DIVERSITY_CEILING`) used **both** to compute the
metric and to render its `calculation_basis` (`COMPACTNESS_BASIS`,
`ROUTE_DIVERSITY_BASIS`). The published provenance is now generated from the same
numbers the arithmetic uses, so the two cannot drift apart again — that is the
durable fix for this whole class.

**The values were deliberately left unchanged**, and both metrics still differ
from `SKILL.md`. Changing `/4`→`/12` or `80/20`→`60/40` would move every delivered
report's numbers, and the evidence does not clearly favour the spec: both code
sites carry comments asserting a deliberate recalibration ("4 = realistic max for
non-baseline routes"), and `calc_compactness`'s docstring argues at length that
position, not stability, is the right quantity — while `SKILL.md:3225` also calls
its own figure "recalibrated". One side is stale and the code does not say which.
**This needs a product decision**, recorded below.

## A7 — `high_danger_pct` is structurally always 0

**Severity: HIGH. Status: FIXED.**

`deep_skill_metrics.py:1355-1360`

```python
high_danger_zones = {"six_yard_box", "penalty_spot"}
high_danger = sum(1 for c in chances
                  if c.get("shot_zone") in high_danger_zones
                  or c.get("origin_row") in high_danger_zones)
```

`shot_zone` is populated from `shot.get("origin_column", "unknown")` (`:1325`) —
a *lateral* vocabulary (`left_channel` / `central` / `right_channel`), disjoint
from the row values being tested. And `origin_row` is never a key on the chance
dicts built at `:1319-1330` / `:1334-1343`.

**Failure:** 8 shots, all from `origin_row="six_yard_box"` → `high_danger=0` → the
report string at `:1373` reads **"8 chances: 0% from high-danger zones (six-yard
box or penalty spot)"**. Both the primary and fallback branch feed the same broken
test.

**Fix applied.** `origin_row` is now carried into the chance dict as `shot_row`
and classified against the row vocabulary only. The denominator changed too: it
was every chance, so unclassifiable ones silently counted as low-danger. It is now
only chances whose row is known, with `chances_with_known_row` published alongside
so a 100% from one shot is distinguishable from 100% across twenty. When no row is
recorded anywhere the percentage is `None` and the summary says so, instead of
reporting a measured 0%. The same file already read `origin_row` correctly at
`:1581-1583`, which is the pattern this now follows.

Verified: 3 shots (2 high-danger) → 67%, classifiable 3/3; no `origin_row` → `None`
with an explicit "not recorded" summary.

## A8 — `_match_state_at_window()` ignores detected kickoff, mislabelling the score

**Severity: HIGH.**

`pipeline_runner_v2.py:858-863` reads `mc.get("ko_1h_s")` and defaults to 0, with
no fallback to `match_boundaries.json`. The file documents the required contract
150 lines later (`:1003-1008`):

> "Boundary sources differ by match-config convention: Bayern-style configs store
> ko_1h_s / ko_2h_s directly; others (Gorleston, Felix) leave those null and stash
> the values in match_boundaries.json. Read from match_config first, fall back to
> match_boundaries.json."

No Python writes `ko_1h_s` anywhere (AST map: zero writers; the `"ko_1h_s"` at
`detect_boundaries.py:298` is a local `_boundary_stats` dict — a name collision),
so this path is taken whenever the operator did not hand-enter the value.

**Failure:** 5 min of pre-match build-up, kickoff correctly detected at 300s and
written to `match_boundaries.json`, `ko_1h_s: null`. A goal at match minute 10 has
true broadcast time 900s; line 882 computes 600s. For a window starting at 700s,
line 894 counts the goal — truth is it has not happened. The window is told the
score is **1-0 when it is 0-0**, and every match-state observation in it is
reasoned from a false scoreline.

**Scope of A8 — one site, not two.** An earlier revision of this document claimed
`pipeline_runner_v2.py:2229-2230` was a second instance. **That was wrong and is
retracted.** Lines 2221-2226 read `match_boundaries.json` *first*, and `:2229` is
the `else` branch; the comment at `:2216` states the intended order outright
("Priority: match_boundaries.json -> window_plan.json -> match_config.json -> 0").
That site is correct — in fact it is the strongest of the three, preferring the
detected boundaries over hand-entered config.

Two of the three readers therefore handle this properly (`:1009-1023` and
`:2221-2231`). **`_match_state_at_window()` at `:858` is the only defective one.**
The fix is correspondingly narrower: bring that one site into line, ideally by
extracting the `:2221-2231` logic into a shared helper.

## A9 — divergent defaults inflate the player-ID confidence claim

**Severity: MEDIUM.**

| Site | Default |
|---|---|
| `build_readiness_check.py:60` (writer) | `"tentative"` |
| `pipeline_runner_v2.py:2926`, `:3038` (report prompts) | `"probable"` |

The writer is conservative, the consumers optimistic. The value is injected as
`PLAYER ID CEILING: {player_id_ceiling}` (`:2982`, `:3100`). On a missing or
truncated readiness file the loader yields `{}` and the report asserts player
identification is *probable* when nothing was established. Reachable: a failed
step is marked failed and execution continues (`:2338-2358`).

## A10 — the pipeline cannot be reproducibly installed

**Severity: MEDIUM (blocks remediation).**

No `requirements.txt`, `pyproject.toml`, `setup.py` or `Pipfile`. Seven
third-party packages, none pinned; four have import names differing from their pip
names, so the dependency set cannot be reconstructed from imports alone:

| Import | pip package | Files |
|---|---|---|
| `anthropic` | `anthropic` | 6 |
| `PIL` | **Pillow** | 4 |
| `dotenv` | **python-dotenv** | 4 |
| `cv2` | **opencv-python** | 3 |
| `numpy` | `numpy` | 1 |
| `docx` | **python-docx** | 1 |
| `easyocr` | `easyocr` | 1 |

`anthropic` is unpinned despite being the core dependency, whose API surface
changes across majors — two machines can produce different analysis from identical
inputs with no recorded cause. `ffmpeg`/`ffprobe` are invoked as external binaries
with no documented version floor. This also blocks the test suite: tests need a
declarable environment first.

## A11 — the spec does not bind steps to scripts (root cause of the drift class)

**Severity: HIGH (structural).**

`SKILL.md` defines 13 pipeline steps across 4,874 lines and mentions **8 distinct
script filenames** out of 43 (verified mechanically:
`grep -o '[a-z_0-9]*\.py' SKILL.md | sort -u`), leaving **35 never referenced by
filename**. It contains **two** runnable command lines — `:3804` and `:4779`.

**Steps 1, 1b, 1c, 1d, 1e and 2 have no module binding at all** — their embedded
code blocks import only stdlib (`json`, `os`, `math`, `cv2`, `requests`). Steps 1a
and 1f *do* reveal their implementing module, but only via an import line inside a
code sample (`from container_analyser import run_step_1a`;
`from source_profiler import build_source_profile`) rather than by naming the
script. An earlier revision of this document said eight steps named nothing; the
correct figure is six, and that is what the finding rests on.

The binding is therefore partial and incidental — it survives in module docstrings
one-directionally (code → step), and in two import lines, never as spec → code.

**Related, surfaced while verifying the above:** the Step 5 command block at
`SKILL.md:4778-4779` hardcodes a machine-specific Windows path —
`C:\Users\dbmux\AppData\Local\...\python.exe` and
`C:\Users\dbmux\.claude\skills\match-analysis\scripts\md_to_docx.py`. F6
("hardcoded absolute paths") was fixed in the *code* but survives in the *spec*,
so the documented Step 5 invocation cannot work for anyone but its author. A
concrete instance of the binding problem: the fix never propagated across the
spec/code boundary because nothing ties the two together.

Every finding in the historical `AUDIT.md` (F1-F5, F15) is a producer and consumer
disagreeing. Nothing declares which script is the producer for a step, so drift
has no point of detection — a step's spec can change with no script changing, and
a script can change without contradicting any written spec. A1, A2, A4, A5, A6 and
A8 are all fresh instances. `ARCHITECTURE.md`, delivered with this audit, supplies
the missing binding.

## A12 — a runnable legacy twin of the accumulator, that the spec points operators at

**Severity: MEDIUM.**

Two modules define `update_running_summary`:

| | Live | Legacy twin |
|---|---|---|
| Location | `accumulator.py:341` | `update_running_summary_v2.py:100` |
| Invoked as | library function | **standalone CLI** (`__main__`, `:238`) |
| Importers | yes | **none** |
| Stamps `schema_version`? | yes | **no** (`:235`) |
| Formation key | canonical `formation.home` | **legacy `formation.shape_in_possession`** |

The twin is dead by import graph but is the only one directly *runnable*,
advertises itself as a "drop-in replacement", and `SKILL.md:4845` sends operators
to it by name ("Re-run update_running_summary for that merged file").

**Failure:** operator follows the documented recovery and runs it. `:128` reads
the superseded formation key — `pipeline_accessors.py:83-84` states the canonical
key is `formation.home` — so every `formation_history` entry gets `shape: None`,
and the rewritten file loses its `schema_version`. Both silent; exit status 0.

**Not a data reset** — verified: `:111-119` load and backfill the existing summary.

## A13 — the dual-agent merge silently drops match state on every event window

**Severity: HIGH.**

`merge_utils.py:490-561` builds its output as a **literal dict allowlist**.
Verified absent from it: `match_state`, `score_home`, `score_away`, `confidence`,
`source_limitations`, `timestamp_range`, `half` — all seven of which the agent
schema emits (`pipeline_runner_v2.py:440-445`, `:558-561`).

By contrast `merge_single_agent` (`:221-278`) starts from the whole parsed dict
and only *adds* keys, so **routine windows keep all seven**. Only event windows —
which take the dual path — lose them.

**Failure:** window 07 is a goal window, so an `agent_07_event.json` exists and
the dual path runs. Agent A emitted `"match_state": "away_winning"`. The merged
file has no `match_state` key, so `accumulator.py:477-494`'s `if ms:` is falsy and
**no row is appended**. On a 20-window match with 4 event windows,
`match_state_by_window` has 16 rows and the holes sit exactly at the goal windows —
the only windows where the score changes. `reconcile_goals_and_state`
(`:1719-1723`) back-fills only rows that exist, so the gaps persist into the report.

`half` survives via a fallback (`escalation_router._infer_half`, `:187-198`). The
other six have no consumer fallback.

## A14 — frame ordering assumes lexical == chronological; breaks at minute 100

**Severity: HIGH.**

Frames are named `f"frame_{m:02d}m{sec:02d}s.jpg"` (`window_plan.py:29-32`,
`detect_boundaries.py:171`). `{m:02d}` emits three digits past minute 99, and the
video clock includes pre-match and half-time, so a 90-minute match video routinely
runs 105–115 minutes.

`pipeline_runner_v2.py:198-201` and `:209-212` order frames with a plain
`sorted()` on filename. Verified:

```
sorted([...97m, 98m, 99m, 100m, 101m, 102m...])
  → ['frame_100m00s.jpg', 'frame_101m00s.jpg', 'frame_102m00s.jpg',
     'frame_97m00s.jpg',  'frame_98m00s.jpg',  'frame_99m00s.jpg']
```

**Failure:** a window spanning video 97m30s–102m30s is handed to the structural
agent as `[100m00s … 102m29s, 97m30s … 99m59s]`, while the prompt tells the agent
the frames are a chronological range (`:1051`). Every build-up chain,
`notable_shifts`, and inter-frame motion read for that window is sequenced
backwards — in second-half stoppage time and extra time, exactly where late
tactical shifts concentrate.

`detect_boundaries.py:455-457` sorts the same filenames with
`key=lambda p: frame_to_seconds(p)`, which is correct — confirming the runner's
lexical sort is an oversight, not a project convention.

## A15 — `datetime.utcnow()` on a deprecation path

**Severity: LOW.**

`setpiece_writeback.py:75, 119, 159, 196`. Deprecated from Python 3.12 and returns
a **naive** datetime; the appended `"Z"` asserts UTC by convention, not by value.
Use `datetime.now(timezone.utc)`. Environment is Python 3.11.15 — latent, not live.

---

# REPORTED

Systematic-sweep findings with file:line evidence and traced failure paths, not
individually re-derived. Verify before fixing.

## Silent failure and false assurance

- **`validation/check_consistency.py:119-143`** — the goal-log invariant is gated
  on goal `description` text, but `accumulator.py:1694-1700` rebuilds `key_moments`
  goals emitting only `type/team/minute/source`; `description` is dropped. Both
  sub-checks skip, no `unverifiable()` is emitted, and the checker prints
  `VERDICT: PASS -- no self-contradiction found` with exit 0.
- **`validation/check_consistency.py:189-203`** — invariant 2 compares the report
  against a value `accumulator.py:1719-1723` already overwrote using the identical
  formula. The raw reads are parked in `match_state_raw_read`, which the checker
  never reads, so `disagreeing` is structurally always 0.
- **`build_readiness_check.py:79-83`** — with no operator event list, the
  `known_events` loop never runs → `events_checked=0, missed=0` → `gt_passed=True`
  → `"event_validation_passed": true` published, terminal prints "All events
  accounted for".
- **`build_readiness_check.py:119-140`** — the blocking branch is guarded on
  `source_prof` being truthy, so profiling *never running* is less blocking than
  profiling running badly; absence is published as `classification_confidence: 0.0`
  alongside `report_ready: true`.
- **`build_readiness_check.py:133`** — `suppressed_families = []` is hardcoded
  with the comment "no suppressed families in this pipeline", while
  `source_profiler.py:165` builds real suppression state. The artefact whose job is
  stating what could not be measured asserts nothing was suppressed.
- **`generate_flagged_moments.py:146`** — `source_type` defaults to
  `"broadcast_fixed_wide"` and is printed as fact at `:216-217`;
  `pipeline_runner_v2.py:2927` defaults the same field to `"veo_ball_tracking"`.
  The two generators contradict each other, so at most one can be right.
- **`validation/compare_counter1.py:176`** — `re.match(r"(1H|2H)_(\d+)", wlabel)`
  fails on the `W02_1H_05-10min` form (`check_consistency.py:177` uses `re.search`
  precisely because of this) → every window scored against a fabricated `"level"`
  truth. Same class as the bug fixed in `af61dfd`, still live here. Also `:134-144`:
  `claimed_count` is hardcoded `0` and the finding string hardcodes
  `"CARDS -- 5 real, 0 claimed"` regardless of input — a fixture-specific value
  emitted as a measurement.
- **`pitch_validation.py:19-23`** — `KNOWN_NON_STANDARD_VENUES` is empty, so the
  loop at `:73` never executes and validation prints OK for any venue. And
  `accumulator.py:518` `_pct_to_m(pct, pitch=105.0)` is never called with a `pitch`
  argument, so a correctly-recorded 100m pitch still yields metre figures computed
  at 105m — ~5% overstatement on every distance.

## Numeric correctness

- **`deep_skill_metrics.py:1092-1128, 1410-1444`** — metrics read `zone_start`/
  `zone_end` while the structural prompt emits `start_zone`/`end_zone`
  (`pipeline_runner_v2.py:474-499`; `accumulator.py:179` confirms the live shape).
  120 real sequences yield `final_third_rate_pct = 0.0` and
  `pattern_reliability = 0.0`, which `calc_predictability` (`:1450`, guards only
  `is None`) turns into `predictability_score = 0.4, category "mixed"` — a prose
  claim derived from zero zone observations. `synthesis_agent.py:710-717` misses it
  because it keys on `final_third_rate` while the emitted key is
  `final_third_rate_pct`.
- **`accumulator.py:550-553`** — `derived_avg_pct = (home_height_pct + away_height_pct)/2`,
  consumed by `deep_skill_metrics.py:1874-1894` and labelled `subject_team: "focus"`.
  Focus team at 30% and opponent at 60% reports **47.3 m** for a side whose line is
  at 31.5 m — a deep block reported as a high line.
- **`accumulator.py:760-779`** — `_count_vertical_progressions` / turnovers applied
  with no team filter, though the possession block 40 lines above (`:717-722`) does
  split by team. Focus 20%/opponent 80% reports `progressive_rate = 0.50` as the
  focus team's build-up effectiveness.
- **`accumulator.py:1318-1319`** — `windows_appeared += 1` sits inside
  `for obs in observations`, so 4 observations in one window count as 4 windows.
  `synthesis_agent.py:990` gates the Player Tendency Table on `>= 3` — a
  single-window burst is published as a cross-match tendency.
- **`accumulator.py:1136-1140`** — the glob unions `*structural*.json` and
  `*_merged.json`, but `merge_utils.py:520` copies `set_pieces` verbatim into the
  merged file, so runners are counted twice (three times with a `*setpiece*.json`
  burst). No dedup anywhere in the function.
- **`deep_skill_metrics.py:1242-1257`** — transition rates pool
  `transitions + flagged_moments + key_moments + resolved confirmations`
  un-deduplicated; only `key_moments` are deduped upstream, never against
  `transitions`. One counter-attack logged three ways clears the `len < 3` gate and
  reports `attack_threat_rate = 1.00, sample_n = 3`.
- **`deep_skill_metrics.py:1471-1476`** — `press_success_rate` divides press wins
  by *all* transitions including those where the team lost possession; understates
  by exactly the share of opponent-possession transitions (0.30 reported where the
  correct base gives 0.60).
- **`deep_skill_metrics.py:1112-1128`** — backward-shift rate divides by all
  windows, including the ones `accumulator.py:529-539` appends with empty
  `defensive_line`. Also `from_pct`/`to_pct` default to `0`, so a shift recording
  only `{"from_pct": 45}` counts as backward — missing data becomes a
  vulnerability event.
- **`deep_skill_metrics.py:1651-1658`** — fitness trajectory compares thirds of the
  *observed* subset but reports the full window count as its sample: 9 readable
  windows of 20 yields a "first-third vs last-third of match" claim with
  `sample_n = 20`, second half entirely unmeasured.
- **`player_aggregator.py:395, 410-423`** — windows with zero observations are
  dropped before the early/late split, so a player who disappears after window 5
  scores arc `"stable"` with `avg_obs_late_windows: 10.0`. `fading_late` can never
  fire for total disappearance.
- **`zone_helpers.py:244-249`** — a missing `off_ball_coverage_score` defaults to
  `0`, tripping the `< 0.4` branch, and `:313-334` writes `between_lines = None`
  back into every `*_merged.json` on disk. `player_aggregator.py:853` defaults the
  same field to `1`, so two modules disagree on the same missing input.

## Spec vs code

- **`escalation_router.py`** — set-piece escalation is the only path that executes
  (`pipeline_runner_v2.py:2410-2414` filters `event_type == "set_piece_delivery"`),
  so goals, transitions, box entries and duels are queued, counted in the
  reliability report, and never re-analysed. `escalated_confirmation` is
  unreachable for every match-scope finding.
- **`deep_skill_metrics.py:1241, 1623`** — filters on `status == "resolved"`, but
  `escalation_router.py:139` only ever writes `status = "queued"` and
  `setpiece_writeback.py:195` sets a separate `resolved` boolean. The condition is
  never true, so escalation can only lower player-metric confidence, never raise it.
- **`escalation_router.py:399-404`** — `all_items = all_high + med_allowed` appends
  medium items *after* the cap; 5 high + 30 medium yields a 35-item queue against
  `SKILL.md:1171`'s cap of 10, with `skipped_by_cap` reporting 0.
  `MAX_MEDIUM_ALLOWED = 4` (`:31`) is declared and never referenced.
- **`escalation_router.py:67`** — `set_piece_delivery` runs at 5fps against
  `SKILL.md:2849`'s 3fps, stamping `reason: "density"`, a value absent from the
  spec's vocabulary (`importance`, `uncertainty`, `fast_event`).
- **`escalation_router.py:60, 53`** — goal padding 6s vs spec 4s; build-up padding
  applied symmetrically (`:132-133`) though `SKILL.md:1167-1168` grants 8s *before*
  only.
- **`escalation_router.py:40-44`** — `ALWAYS_ESCALATE_TYPES` omits shot types, so
  "Shot on target" (`SKILL.md:2845`, always-escalate) is dropped as
  `skipped_ineligible` at `:364`.
- **`escalation_router.py:103-118`** — of the five spec triggers
  (`SKILL.md:1151-1156`), "visibility poor" and "two competing interpretations" are
  enforced by no code; neither `source_profile.json` nor `visibility_scores` is
  opened in the file. The confidence trigger is reachable only in the
  `rule_key is None` branch.
- **`source_profiler.py:195-198`** — "Only downgrade, never upgrade via visibility
  rules" makes the sole upgrade rule in `source_profiles_config.json`
  (`if_full_pitch_visibility_above_0.7`) dead. A Veo match at 0.9 full-pitch
  visibility keeps `shape`, `spacing`, `territory` downgraded.
- **`suppressed` is unreachable** — across all ten source profiles (28 families
  each) only `allowed` and `downgraded` appear; `build_readiness_check.py:133`
  hardcodes the empty list. `SKILL.md:1194-1197` names `downgraded` twice and never
  names `suppressed`, while `SKILL.md:1217` mandates suppression for ball-follow
  footage.
- **`deep_skill_metrics.py:960-961` vs `:995-996`** — the v2 path penalises
  `0 < windows < 3` while the v3 helper penalises `< 3`, so a metric built from
  **zero** windows keeps full base confidence while a two-window metric is
  penalised. The worst-evidence case scores higher.
- **`deep_skill_metrics.py:59-75`** — `broadcast_tv` capped at 0.5 for *every*
  metric where `SKILL.md:3313-3315` asks only for structural downgrade,
  guaranteeing `avg_confidence < 0.5` — the exact threshold `SKILL.md:3340` tells
  the operator to read as "significant source limitations". `broadcast_fixed_wide`
  is an eleventh source type against the spec's ten and is absent from
  `source_profiler.py` `VALID_SOURCE_TYPES`, so it is rewritten to `unknown`
  (`:397-400`) and capped at 0.5.
- **`deep_skill_metrics.py:2463-2470`** — three of five spec'd player metrics
  (`player_decision_profile`, `player_movement_contribution`,
  `player_duel_effectiveness`, `SKILL.md:3249-3251`) are never computed; they are
  absent rather than reported as unavailable, a distinction `SKILL.md:3200` draws
  explicitly.
- **`player_escalation_router.py`** — an entire player-confirmation subsystem with
  its own vocabulary (`:32-38`), fps map (`:40-46`) and cap (`:48`) runs outside the
  spec, which states at `SKILL.md:1262-1264` that player passes are "not yet
  implemented". Windows are ±2s against a ±5s default, `foot_used` runs at 3fps
  against a 5-10fps floor, and results are still stamped `escalated_confirmation`
  (`:179`). `:180` writes confidence as a **string** (`"high"`/`"medium"`) against
  `SKILL.md:1304`'s float.

## Merge and path integrity

- **`merge_utils.py:402-405`** — `key_moments` dedup by minute string does two
  things at once: the `if _moment_key(m)` filter **discards** any moment with a null
  minute (which the prompt explicitly instructs agents to emit —
  `pipeline_runner_v2.py:718`, `:570`), and the dict comprehension is **last-wins**,
  so a goal and a card at `"38:00"` collapse to one. The same agent output on a
  routine window keeps all three, because `merge_single_agent` never touches
  `key_moments`. A dropped goal becomes a `missed_by_footage` entry against
  operator facts (`accumulator.py:1640-1641`).
- **`merge_utils.py:320-338, 490-561`** — the dual-agent "consensus layer" reads
  `defensive_line`, `formation`, `pressing`, `pass_sequences`, `gk_distribution`
  from agent B, none of which the event agent emits (its contract is
  `event_agent`, `events[]`, `shape_vs_structural_agent{}` —
  `pipeline_runner_v2.py:1717`), and the output allowlist has no slot for either
  of the keys it *does* emit. The event agent's entire payload is discarded, and
  because `merge_categorical(fm_a_home, None)` returns A unchanged, `merge_note` is
  stamped **`"agreed"`** — a fabricated consensus, while the real
  `shape_vs_structural_agent.disputes` content is never recorded anywhere. Shot
  detail survives only via `accumulator._load_event_output` into `shots_log.json`.
- **`accumulator.py:2026-2037`** — `f"*{window_id}*setpiece*.json"` matches other
  windows by substring: `*03*setpiece*.json` matches
  `agent_12_03m20s_setpiece.json` because the *anchor timestamp* contains "03"
  (verified). It then takes `matches[0]` from an **unsorted** `glob.glob`
  (contrast `pipeline_paths.find_agent_output:117`, which sorts first), and its
  exact-path alternative `agent_{window_id}_setpiece.json` is never produced by the
  burst writer, so the primary always degrades to the loose glob. The enrichment
  cannot fire regardless: `:2181-2192` reads `sp_output.get("outcome")` but the
  burst schema puts `outcome` inside `confirmed_fields`, so every set-piece goal in
  `shots_log.json` stays `evidence_grade: "C"`.
- **`escalation_router.py:386-395`** — the cap is a **prefix slice in chronological
  order** with no priority or confidence sort: `total_high[:STANDARD_CAP_HIGH]`
  over items in `agent_01…agent_18` order. Nine 1H items displace five 2H items
  regardless of `evidence_tier` — a `suggestive` box entry at 04' beats an
  escalation-eligible counterpress at 78' purely on window order. Goals are
  protected; nothing else is.
- **`player_escalation_router.py:109`** — keys the burst on the window *label*,
  which contains a literal **EN DASH U+2013** (`window_plan.py:269-272`;
  `merge_utils.py:620` sanitises only spaces and colons). The resulting Batch API
  `custom_id` is non-ASCII, which the API restricts to `[a-zA-Z0-9_-]`, so the
  whole `3i_player_action` batch submission fails — swallowed by the `except
  Exception` at `pipeline_runner_v2.py:2693`. The sibling router was fixed for
  exactly this (`escalation_router.py:288`, `window = w.get("agent_id") or ...`,
  with a comment explaining why); this one never got the flip. **Latent** — the
  queue is empty on the current corpus.

**Verified as no longer present:** the old audit's merged-filename mismatch
(`agent_{agent_id}_{safe_label}_merged.json` written, `agent_{window_id}_merged.json`
read) **does not exist anywhere in the current tree** — every merged-file read now
goes through a glob. The only surviving exact-path-that-cannot-match is the
set-piece one above.

---

## Disposition of the 2026-05 audit (F1–F15)

`AUDIT.md` is dated 2026-05-14, last touched 2026-05-20. **72 commits** have
landed since, and it audits two files that no longer exist (`pipeline_runner.py`,
`shots_log_addition.py` — the v1 pipeline). Read it as history, not a defect list.

| | Finding | Disposition |
|---|---|---|
| F1 | window_id/agent_id reversed fallback | **Fixed** — `pipeline_accessors.py` |
| F4 | `marking` vs `marking_system` | **Fixed** — `get_marking()`, `:77` |
| F5 | formation schema drift | **Fixed** in live path; legacy twin still reads old key → **A12** |
| F6 | hardcoded absolute paths | **Fixed** |
| F9 | `start_s`/`start_seconds` | **Fixed** — `get_window_start_seconds()` |
| F11 | reset omits `set_pieces_rejected` | **Fixed** — `accumulator.py:401` |
| F12 | no `schema_version` | **Fixed** — `pipeline_schemas.py`, 69 sites |
| F2, F3, F7, F8, F10, F13, F14, F15 | — | superseded by v1 removal, or pending re-verification |

The remedy the team applied — a canonical accessor module — was the right call.
**A4, A8 and A9 are cases of that pattern being bypassed**, not of it failing. The
gap is enforcement, not design.

---

## Checked and dropped

- **`marking`/`marking_system` order at `pipeline_runner_v2.py:1847`** reads
  legacy-first, inverting canonical `get_marking()`. **Not a bug:** `:1844`'s
  comment shows the intent is to surface the *pre-burst 1fps* reading for the agent
  to confirm or correct, so preferring the 1fps key is correct. The re-run hazard is
  closed by the idempotency guard at `setpiece_writeback.py:64`.
- **`update_running_summary_v2.py` resetting the summary.** Hypothesised total data
  loss; **false** — `:111-119` load and backfill. The narrower real defect is A12.

---

## Architectural notes

- **`running_summary.json` has four live writers** (`accumulator.py:812`, `:1915`,
  `:1931`, and `setpiece_writeback.py:181`), none atomic — plain `open(path,"w")` +
  `json.dump`, no write-temp-then-rename. `accumulator.py:1931` is the **final**
  write of a normal run: it persists `_backfill_match_state_window_labels`,
  `reconcile_goals_and_state` and `set_piece_filter_summary`, so it sets the file's
  end state. An interruption truncates the artefact every downstream stage reads.
  No concurrency exists today, so this is a durability risk, not a live race.
- **Two representations of kickoff** — nested `boundaries.ko_1h.seconds` in
  `match_boundaries.json` (written by code) vs flat `ko_1h_s`/`ht_s`/`ko_et1_s` in
  `match_config.json` (**no Python writer — hand-entered**). Direct cause of A8.
  `ht_s`, `ko_et1_s`, `ko_et2_s` have no writer anywhere.
- **158 of 1,245 dict keys are read by Python but never written by it** — they
  arrive from LLM agents following `SKILL.md`. A field can go missing because the
  spec changed, with no Python change and no test that would notice.

---

## Recommended order of work

**Decisions needed from the product owner** (blocking further metric work):

- **A5/A6 values.** Is route-diversity's ceiling 4 or 12? Is compactness 80/20 on
  line *position*, or 60/40 on line *stability*? Whichever wins, the loser's
  document should be updated in the same change.
- **A3 metrics.** `SKILL.md:3247-3248` specifies two rating-derived player metrics
  while `:1630`/`:4196` forbid ratings. Permit a bounded rating, or retire the two
  metrics?
- **A2 contract.** Should agents emit a `findings` array, or should the merge-stage
  stamping be deleted?

**Tier 1 — the pipeline currently publishes claims it cannot support.**

1. **A3** — delete the fabricated `rating` default; the two metrics must be
   `None`/unavailable, not constants. Smallest diff, worst current output.
2. **A1** — wire the readiness gate; make Phase 5 refuse to run on `report_ready: false`.
3. **A2** — decide whether the Universal Finding Object is the contract. If yes,
   the agent schema must emit `findings`; if no, delete the gate-stamping machinery
   that pretends it does. Either is fine — the present half-state is not.
4. **A5, A6, A7** — fix the metrics, and treat any divergence between a formula and
   its `calculation_basis` string as a build error.

**Tier 2 — data loss and wrong derived numbers.**

5. **A13** — add the seven missing keys to the dual-merge allowlist, or rebuild it
   the way `merge_single_agent` does (start from the full dict, add rather than
   enumerate). One-line class of fix, removes a whole family of event-window loss.
6. **A14** — sort frames by parsed timestamp, reusing
   `detect_boundaries.frame_to_seconds`. Affects every match over 100 video minutes.
7. **A8** — one site only: `_match_state_at_window()` at `:858`. Extract a shared
   `resolve_kickoffs(match_dir, mc, wp)` from the correct implementation at
   `:2221-2231` and route `:858` through it. The other two sites already behave.
8. **A4** — route `ground_truth.py` through the canonical moment-key accessor.
9. **A9** — make the default `"tentative"` in all three places.
10. Work the REPORTED numeric and merge lists, verifying each before fixing.

**Tier 3 — structural.**

11. **A10** — pinned `requirements.txt`; unblocks the test suite.
12. **A12** — delete both legacy `update_running_summary_v2*` modules; fix `SKILL.md:4845`.
13. **A11** — put script names and artefacts into `SKILL.md` step headings so spec
    and code can be diffed.

---

## Coverage

All 43 Python modules were included in the mechanical AST sweep — every JSON
read/write site, dict-key write/read, and path literal extracted and diffed
(1,245 distinct keys). `SKILL.md` was analysed structurally and by targeted
section across four independent passes.

Four independent sweeps were run — numeric correctness, silent failure, spec vs
code, and merge/path integrity — each with its own evidence requirements, and
their findings were cross-checked against the mechanical map.

**Not assessed:** runtime behaviour of any kind (no match data, no video, no
installable environment); the analysis agents' prompt effectiveness; and the
correctness of the tactical reasoning itself, as distinct from its data custody.
